using System.Collections;
using UnityEngine;
using View;

public class EntryPoint : MonoBehaviour
{
    [SerializeField] private Timer timer;

    // IEnumerator Start()
    // {
    //     yield return null; 

    //     if (timer != null)
    //     {
    //         timer.EndOfTimeEvent.AddListener(() => OnTimerEnd());
    //     }

    //     private void OnTimerEnd()
    //     {
    //         timer.Reset();
    //     }
    // }
}